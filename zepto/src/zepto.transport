define(function(require) {

  var _zepto = window.Zepto;
  var _$ = window.$;

  // @include https://raw.github.com/madrobby/zepto/v1.0/src/zepto.js

  window.Zepto = _zepto;
  window.$ = _$;

  return Zepto;
});
